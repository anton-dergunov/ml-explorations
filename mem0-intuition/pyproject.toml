[project]
name = "mem0-intuition"
version = "0.1.0"
description = "Watching an LLM memory layer extract, consolidate and retrieve, one call at a time"
requires-python = ">=3.11"
dependencies = [
    "mem0ai>=0.1.100",
    "google-genai>=1.0",
    "python-dotenv>=1.0",
    "ipykernel>=6.29",
    "jupytext>=1.16",
    "pandas>=2.2",
]
